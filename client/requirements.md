## Packages
framer-motion | Page transitions and hover animations
react-markdown | Rendering article content (Markdown)
clsx | Utility for constructing className strings conditionally
tailwind-merge | Utility for merging Tailwind CSS classes
date-fns | Date formatting

## Notes
Razorpay script (https://checkout.razorpay.com/v1/checkout.js) needs to be loaded dynamically for payments.
Auth uses Replit Auth flow but fetches user data from /api/user/me.
Images use Unsplash placeholders for now.
